---
import FlecheIcon from "../../assets/icones/fleche.svg";

export interface Props {
  size?: "mobile" | "desktop";
  direction?: "droite" | "gauche" | "haut" | "bas";
  href?: string;
  onClick?: () => void;
  colorGradient?: { from: string; to: string };
}

const {
  size = "mobile",
  direction = "droite",
  href = "#",
  onClick,
  colorGradient,
} = Astro.props;

// Tailles dynamiques
const sizes = {
  mobile: {
    container: "w-[33px] h-[63px]",
  },
  desktop: {
    container: "w-[58px] h-[111px]",
  },
};

const currentSize = sizes[size] || sizes.desktop;

const arrowColor = colorGradient?.to || "#e1e2e6";
const arrowStrokeColor = colorGradient?.to || "var(--color-light-blue)";
---

<div class={`${currentSize.container} relative`}>
  <a
    href={href}
    class="fleche cursor-pointer transition-all duration-200 hover:scale-110 block origin-center absolute inset-0"
    data-onclick={onClick ? "true" : undefined}
  >
    <svg
      class={`w-full h-full fleche-svg ${direction === "gauche" ? "rotate-180" : direction === "haut" ? "-rotate-90" : direction === "bas" ? "rotate-90" : ""}`}
      viewBox="0 0 61 112"
      fill="none"
      xmlns="http://www.w3.org/2000/svg"
    >
      <path
        d="M3.46201 2.2401L58.2341 54.73C58.6792 55.1566 58.6792 55.8682 58.2341 56.2948L3.46201 108.785C2.71111 109.504 1.47613 108.856 1.64254 107.829L10.1227 55.5124L1.64254 3.19588C1.47613 2.16923 2.71111 1.52049 3.46201 2.2401Z"
        fill={arrowColor}
        stroke={arrowStrokeColor}
        stroke-width="1"
        stroke-linejoin="round"
        stroke-linecap="round"></path>
    </svg>
  </a>
</div>

<script>
  document.addEventListener("DOMContentLoaded", () => {
    const fleche = document.querySelector("a[data-onclick='true']");
    if (fleche) {
      fleche.addEventListener("click", () => {
        console.log("Fleche cliquée");
      });
    }
  });
</script>

<style>
  .fleche-svg {
    transition: filter 0.3s ease;
    filter: drop-shadow(0 0 8px rgba(0, 0, 0, 0.4))
      drop-shadow(0 0 3px rgba(0, 0, 0, 0.3));
  }

  .fleche:hover .fleche-svg {
    filter: drop-shadow(0 0 8px rgba(0, 0, 0, 0.4))
      drop-shadow(0 0 3px rgba(0, 0, 0, 0.3))
      drop-shadow(0 0 15px color-mix(in srgb, var(--color-light-blue) 80%, transparent));
  }

  [data-theme="dark"] .fleche-svg {
    filter: grayscale(100%) brightness(3) invert(1) contrast(0.9)
      drop-shadow(0 0 8px rgba(0, 0, 0, 0.4))
      drop-shadow(0 0 3px rgba(0, 0, 0, 0.3)) !important;
  }

  [data-theme="dark"] .fleche:hover .fleche-svg {
    filter: grayscale(100%) brightness(3.2) invert(1) contrast(0.9)
      drop-shadow(0 0 15px rgba(212, 212, 212, 0.8))
      drop-shadow(0 0 8px rgba(0, 0, 0, 0.4))
      drop-shadow(0 0 3px rgba(0, 0, 0, 0.3)) !important;
  }
</style>
